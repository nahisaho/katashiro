# KATASHIRO v0.4.0 要件定義書（EARS形式）

> **作成日**: 2025-01-13  
> **バージョン**: 0.4.0  
> **ステータス**: Draft  
> **要件形式**: EARS (Easy Approach to Requirements Syntax)

## 📋 概要

本ドキュメントは、Manus（https://manus.im）およびOpenHands（https://openhands.dev）の競合分析に基づき、KATASHIRO v0.4.0で実装すべき機能をEARS形式で定義します。

### EARS パターン凡例

| パターン | 構文 | 用途 |
|---------|------|------|
| **Ubiquitous** | The [system] shall [action] | 常に適用される要件 |
| **Event-Driven** | When [event], the [system] shall [action] | イベント発生時の要件 |
| **State-Driven** | While [state], the [system] shall [action] | 特定状態での要件 |
| **Unwanted** | If [condition], then the [system] shall [action] | 異常系・例外処理 |
| **Optional** | Where [feature], the [system] shall [action] | オプション機能 |
| **Complex** | 上記の組み合わせ | 複合条件の要件 |

---

## � 用語定義

| 用語 | 定義 |
|------|------|
| **Complex Task** | 単一のツール呼び出しでは完了できず、複数のサブタスクへの分解が必要なタスク。例：「市場調査を行い、競合分析レポートを作成する」 |
| **Sub-Agent** | メインタスクから分解されたサブタスクを実行する独立したエージェントインスタンス |
| **Context Pollution** | 複数のサブタスクの情報が混在し、エージェントの判断精度が低下する状態。各サブエージェントが独立コンテキストを持つことで回避 |
| **Action** | ツールへの入力パラメータを定義した型安全なオブジェクト |
| **Observation** | ツール実行結果を定義した型安全なオブジェクト |
| **Workspace** | エージェントがファイル操作を行う作業領域の抽象化 |
| **Risk Level** | アクションの危険度を4段階（low/medium/high/critical）で評価した値 |
| **DAG** | Directed Acyclic Graph。タスク間の依存関係を表現するデータ構造 |

---

## �🔍 競合分析サマリー

### Manus の主要機能

| 機能 | 説明 | katashiro対応 |
|------|------|--------------|
| **Wide Research** | 並列マルチエージェントによる大規模リサーチ（数百項目を同時処理） | ❌ 未対応 |
| **Browser Operator** | ブラウザ自動操作（ログイン済みセッション利用可能） | ❌ 未対応 |
| **マルチエージェントオーケストレーション** | 主エージェント→サブエージェント分散処理 | ❌ 未対応 |
| **コンテキスト過負荷回避** | 各サブエージェントが独立コンテキストで動作 | ⚠️ Wake-Sleepで部分対応 |
| **構造化出力（レポート/データセット）** | 調査結果を構造化フォーマットで出力 | ✅ ReportGenerator |
| **API提供** | REST API経由でのアクセス | ✅ MCP Server |

### OpenHands の主要機能

| 機能 | 説明 | katashiro対応 |
|------|------|--------------|
| **Software Agent SDK** | Python/TypeScript SDKでエージェント構築 | ✅ TypeScript SDK |
| **CodeActAgent** | コード実行統合（bash + Python）エージェント | ❌ 未対応 |
| **Tool System** | Action-Observation パターンによるツール定義 | ⚠️ 部分対応 |
| **MCP Integration** | Model Context Protocol統合 | ✅ MCP Server |
| **Sandbox実行** | Docker/Kubernetes上での隔離実行 | ❌ 未対応 |
| **LocalWorkspace/RemoteWorkspace** | ローカル/リモートワークスペース切替 | ❌ 未対応 |
| **CLI** | コマンドラインインターフェース | ✅ CLI対応 |
| **GUI（Local/Cloud）** | Web UIでのエージェント操作 | ⚠️ VS Code拡張のみ |
| **Conversation管理** | 会話状態の永続化と復元 | ⚠️ Wake-Sleepで部分対応 |
| **Security Analyzer** | アクション実行前のリスク評価 | ❌ 未対応 |
| **Condenser** | トークン管理のための会話圧縮 | ⚠️ Wake-Sleepで部分対応 |

---

## 🎯 ギャップ分析

### 重大なギャップ（優先度: High）

1. **マルチエージェント並列実行**
   - Manus: 数百のサブエージェントを並列実行
   - OpenHands: -
   - katashiro: 単一エージェントのみ

2. **コード実行サンドボックス**
   - Manus: VM上での隔離実行
   - OpenHands: Docker/Kubernetes sandbox
   - katashiro: 実行環境なし

3. **ブラウザ自動操作**
   - Manus: Browser Operator（Chrome拡張）
   - OpenHands: Web Browsing Tool
   - katashiro: Scraperのみ（自動操作なし）

4. **タスク分解・計画**
   - Manus: 自動タスク分解→サブタスク並列実行
   - OpenHands: Task planning/decomposition
   - katashiro: なし

### 中程度のギャップ（優先度: Medium）

5. **Action-Observation パターン**
   - OpenHands: 型安全なツールシステム（Pydantic）
   - katashiro: 部分的実装

6. **ワークスペース管理**
   - OpenHands: Local/Docker/Remote Workspace切替
   - katashiro: なし

7. **セキュリティ分析**
   - OpenHands: アクション実行前のリスク評価
   - katashiro: なし

8. **GUI（Web UI）**
   - Manus: フルWeb UI
   - OpenHands: React SPA
   - katashiro: VS Code拡張のみ

### 軽微なギャップ（優先度: Low）

9. **Slack/Jira/Linear統合**
   - OpenHands Cloud: 各種連携
   - katashiro: なし

10. **チーム機能/RBAC**
    - OpenHands: マルチユーザー、権限管理
    - katashiro: なし

---

## � トレーサビリティマトリクス

| ギャップ# | ギャップ名 | 要件ID | 優先度 |
|-----------|-----------|--------|--------|
| 1 | マルチエージェント並列実行 | REQ-006 | High |
| 2 | コード実行サンドボックス | REQ-007 | High |
| 3 | ブラウザ自動操作 | REQ-008 | High |
| 4 | タスク分解・計画 | REQ-009 | High |
| 5 | Action-Observation パターン | REQ-010 | Medium |
| 6 | ワークスペース管理 | REQ-011 | Medium |
| 7 | セキュリティ分析 | REQ-012 | Medium |
| 8 | GUI（Web UI） | - | v0.5.0予定 |
| 9 | Slack/Jira/Linear統合 | - | v0.5.0予定 |
| 10 | チーム機能/RBAC | - | v0.6.0予定 |

---

## 📝 v0.4.0 機能要件（EARS形式）

### REQ-006: マルチエージェントオーケストレーション

**優先度**: High  
**カテゴリ**: 新機能  
**パッケージ**: `@nahisaho/katashiro-orchestrator`（新規）

#### EARS形式要件

| ID | パターン | 要件文 |
|----|---------|--------|
| REQ-006-01 | Ubiquitous | The Orchestrator shall decompose any complex task (requiring 2+ tool calls) into independent sub-tasks. |
| REQ-006-02 | Event-Driven | When a task is submitted, the Orchestrator shall spawn sub-agents for each sub-task within 1 second. |
| REQ-006-03 | State-Driven | While sub-agents are running, the Orchestrator shall manage 1-100 concurrent executions (configurable, default: 10). |
| REQ-006-04 | Ubiquitous | The Orchestrator shall ensure each sub-agent operates with an isolated context to prevent context pollution. |
| REQ-006-05 | Event-Driven | When all sub-agents complete, the Orchestrator shall aggregate results and remove duplicates. |
| REQ-006-06 | Unwanted | If a sub-agent fails, then the Orchestrator shall continue processing remaining sub-agents and report partial results. |

#### 技術仕様

```typescript
interface OrchestratorConfig {
  maxConcurrentAgents: number;  // デフォルト: 10
  taskDecomposer: TaskDecomposer;
  resultAggregator: ResultAggregator;
}

interface SubAgent {
  id: string;
  task: Task;
  context: AgentContext;
  status: 'pending' | 'running' | 'completed' | 'failed';
}

class MultiAgentOrchestrator {
  constructor(config: OrchestratorConfig);
  async execute(task: string): Promise<AggregatedResult>;
  async decompose(task: string): Promise<Task[]>;
  async spawnSubAgents(tasks: Task[]): Promise<SubAgent[]>;
  async aggregate(results: SubAgentResult[]): Promise<AggregatedResult>;
}
```

#### 受け入れ基準

- [ ] 10個以上のサブエージェントを並列実行可能
- [ ] サブエージェント間でコンテキスト汚染が発生しない
- [ ] 一部のサブエージェント失敗時も全体処理が継続可能
- [ ] 結果統合でデータの重複除去が行われる

---

### REQ-007: コード実行サンドボックス

**優先度**: High  
**カテゴリ**: 新機能  
**パッケージ**: `@nahisaho/katashiro-sandbox`（新規）

#### EARS形式要件

| ID | パターン | 要件文 |
|----|---------|--------|
| REQ-007-01 | Ubiquitous | The Sandbox shall execute code in an isolated Docker/VM environment. |
| REQ-007-02 | Ubiquitous | The Sandbox shall support bash, Python, and JavaScript/TypeScript execution. |
| REQ-007-03 | Event-Driven | When execution time exceeds the timeout (configurable 1-300 seconds, default: 30 seconds), the Sandbox shall terminate the process and return a timeout error. |
| REQ-007-04 | State-Driven | While code is executing, the Sandbox shall enforce CPU and memory limits. |
| REQ-007-05 | Ubiquitous | The Sandbox shall provide a temporary file system for code execution. |
| REQ-007-06 | Unwanted | If code attempts to access the host system, then the Sandbox shall block the access and log the attempt. |

#### 技術仕様

```typescript
interface SandboxConfig {
  runtime: 'docker' | 'vm' | 'wasm';
  timeout: number;  // ミリ秒
  memoryLimit: string;  // e.g., '512m'
  cpuLimit: number;  // e.g., 0.5
  workingDir: string;
}

interface ExecutionResult {
  exitCode: number;
  stdout: string;
  stderr: string;
  duration: number;
  files: FileOutput[];
}

class SandboxExecutor {
  constructor(config: SandboxConfig);
  async execute(code: string, language: Language): Promise<ExecutionResult>;
  async cleanup(): Promise<void>;
}
```

#### 受け入れ基準

- [ ] Dockerコンテナ内でbashコマンドが実行可能
- [ ] Python/Node.jsスクリプトが実行可能
- [ ] タイムアウト時に適切に終了
- [ ] ホストシステムへのアクセスが完全に遮断

---

### REQ-008: ブラウザ自動操作

**優先度**: High  
**カテゴリ**: 新機能  
**パッケージ**: `@nahisaho/katashiro-collector`（拡張）

#### EARS形式要件

| ID | パターン | 要件文 |
|----|---------|--------|
| REQ-008-01 | Ubiquitous | The BrowserOperator shall control a headless browser using Puppeteer or Playwright. |
| REQ-008-02 | Event-Driven | When a navigation action is requested, the BrowserOperator shall navigate to the specified URL and wait for page load. |
| REQ-008-03 | Event-Driven | When an interaction action is requested, the BrowserOperator shall perform click, type, scroll, or wait operations on the specified selector. |
| REQ-008-04 | Event-Driven | When a screenshot action is requested, the BrowserOperator shall capture the page or element and return the image buffer. |
| REQ-008-05 | Event-Driven | When a JavaScript evaluation is requested, the BrowserOperator shall execute the script in the page context and return the result. |
| REQ-008-06 | Unwanted | If an element is not found within timeout, then the BrowserOperator shall return an error with the selector details. |

#### 技術仕様

```typescript
interface BrowserAction {
  type: 'goto' | 'click' | 'type' | 'scroll' | 'wait' | 'screenshot' | 'evaluate';
  selector?: string;
  value?: string;
  options?: Record<string, unknown>;
}

interface BrowserObservation {
  success: boolean;
  data?: unknown;
  screenshot?: Buffer;
  error?: string;
}

class BrowserOperator {
  constructor(options: BrowserOptions);
  async launch(): Promise<void>;
  async execute(action: BrowserAction): Promise<BrowserObservation>;
  async close(): Promise<void>;
}
```

#### 受け入れ基準

- [ ] URLへの遷移が可能
- [ ] フォーム入力とボタンクリックが可能
- [ ] スクリーンショットが取得可能
- [ ] JavaScript実行結果を取得可能

---

### REQ-009: タスク分解・計画

**優先度**: High  
**カテゴリ**: 新機能  
**パッケージ**: `@nahisaho/katashiro-planner`（新規）

#### EARS形式要件

| ID | パターン | 要件文 |
|----|---------|--------|
| REQ-009-01 | Event-Driven | When a complex task (requiring 2+ tool calls) is submitted, the TaskPlanner shall decompose it into 2-50 executable sub-tasks. |
| REQ-009-02 | Ubiquitous | The TaskPlanner shall identify dependencies between sub-tasks and determine execution order. |
| REQ-009-03 | Event-Driven | When sub-tasks are identified, the TaskPlanner shall generate a DAG-based execution plan. |
| REQ-009-04 | Unwanted | If a task fails during execution, then the TaskPlanner shall generate an alternative plan for remaining tasks. |
| REQ-009-05 | State-Driven | While tasks are executing, the TaskPlanner shall track and report progress status in real-time. |

#### 技術仕様

```typescript
interface Task {
  id: string;
  description: string;
  dependencies: string[];
  estimatedDuration: number;
  priority: number;
}

interface ExecutionPlan {
  tasks: Task[];
  dag: DAG<Task>;
  estimatedTotalDuration: number;
}

class TaskPlanner {
  async decompose(userRequest: string): Promise<Task[]>;
  async plan(tasks: Task[]): Promise<ExecutionPlan>;
  async replan(plan: ExecutionPlan, failedTask: Task): Promise<ExecutionPlan>;
}
```

#### 受け入れ基準

- [ ] 自然言語のタスクを5個以上のサブタスクに分解可能
- [ ] 依存関係に基づいた実行順序が正しく決定される
- [ ] 失敗時に代替計画が生成される
- [ ] 進捗状況がリアルタイムで取得可能

---

### REQ-010: Action-Observation型安全ツールシステム

**優先度**: Medium  
**カテゴリ**: 改善  
**パッケージ**: `@nahisaho/katashiro-core`（拡張）

#### EARS形式要件

| ID | パターン | 要件文 |
|----|---------|--------|
| REQ-010-01 | Ubiquitous | The ToolSystem shall validate all action inputs using Zod schemas before execution. |
| REQ-010-02 | Ubiquitous | The ToolSystem shall define type-safe observation schemas for all tool outputs. |
| REQ-010-03 | Ubiquitous | The ToolExecutor shall transform validated Actions into typed Observations. |
| REQ-010-04 | Event-Driven | When a tool is requested by name, the ToolRegistry shall resolve and return the corresponding ToolDefinition. |
| REQ-010-05 | Optional | Where tool annotations are provided, the ToolSystem shall expose hints (readOnly, destructive, idempotent, openWorld) to LLM prompts. |
| REQ-010-06 | Unwanted | If action validation fails, then the ToolSystem shall return a typed error with validation details. |

#### 技術仕様

```typescript
import { z } from 'zod';

// Action定義
const SearchAction = z.object({
  query: z.string(),
  maxResults: z.number().default(10),
});
type SearchAction = z.infer<typeof SearchAction>;

// Observation定義
const SearchObservation = z.object({
  results: z.array(SearchResultSchema),
  count: z.number(),
  duration: z.number(),
});
type SearchObservation = z.infer<typeof SearchObservation>;

// ToolExecutor
interface ToolExecutor<A, O> {
  (action: A): Promise<O>;
}

// ToolDefinition
interface ToolDefinition<A, O> {
  name: string;
  description: string;
  actionSchema: z.ZodType<A>;
  observationSchema: z.ZodType<O>;
  executor: ToolExecutor<A, O>;
  annotations?: ToolAnnotations;
}

interface ToolAnnotations {
  readOnlyHint?: boolean;
  destructiveHint?: boolean;
  idempotentHint?: boolean;
  openWorldHint?: boolean;
}
```

#### 受け入れ基準

- [ ] 全既存ツールがAction-Observation形式に移行
- [ ] 入力バリデーションエラーが型安全に処理される
- [ ] ToolAnnotationsがLLMプロンプトに反映される
- [ ] 新規ツール追加が5分以内で可能

---

### REQ-011: ワークスペース管理

**優先度**: Medium  
**カテゴリ**: 新機能  
**パッケージ**: `@nahisaho/katashiro-workspace`（新規）

#### EARS形式要件

| ID | パターン | 要件文 |
|----|---------|--------|
| REQ-011-01 | Ubiquitous | The Workspace shall provide a unified interface for file operations (read, write, list, search, delete). |
| REQ-011-02 | Optional | Where LocalWorkspace is configured, the Workspace shall operate on the local file system. |
| REQ-011-03 | Optional | Where RemoteWorkspace is configured, the Workspace shall operate on a remote server via API. |
| REQ-011-04 | Optional | Where DockerWorkspace is configured, the Workspace shall operate within a Docker container. |
| REQ-011-05 | Ubiquitous | The Workspace shall allow tools to operate identically regardless of workspace type. |
| REQ-011-06 | Event-Driven | When a glob search is requested, the Workspace shall return matching file paths within 1 second for up to 10,000 files. |

#### 技術仕様

```typescript
interface Workspace {
  readonly type: 'local' | 'remote' | 'docker';
  readonly workingDir: string;
  
  read(path: string): Promise<string>;
  write(path: string, content: string): Promise<void>;
  list(path: string): Promise<FileInfo[]>;
  search(pattern: string): Promise<string[]>;
  exists(path: string): Promise<boolean>;
  delete(path: string): Promise<void>;
}

class LocalWorkspace implements Workspace { ... }
class RemoteWorkspace implements Workspace { ... }
class DockerWorkspace extends RemoteWorkspace { ... }
```

#### 受け入れ基準

- [ ] LocalWorkspaceでファイルの読み書きが可能
- [ ] DockerWorkspace内でのファイル操作が可能
- [ ] ワークスペースの種類を変更してもツールコードが変わらない
- [ ] ファイル検索（glob）が高速に動作

---

### REQ-012: セキュリティ分析

**優先度**: Medium  
**カテゴリ**: 新機能  
**パッケージ**: `@nahisaho/katashiro-security`（新規）

#### EARS形式要件

| ID | パターン | 要件文 |
|----|---------|--------|
| REQ-012-01 | Event-Driven | When an action is submitted, the SecurityAnalyzer shall evaluate the risk level before execution. |
| REQ-012-02 | State-Driven | While risk level is 'high' or 'critical', the SecurityAnalyzer shall require user confirmation before proceeding. |
| REQ-012-03 | Unwanted | If an action matches a deny pattern, then the SecurityAnalyzer shall block execution and return an error. |
| REQ-012-04 | Event-Driven | When an action matches an allow pattern, the SecurityAnalyzer shall permit execution without confirmation. |
| REQ-012-05 | Ubiquitous | The SecurityAnalyzer shall log all action executions with timestamp, action details, and result. |
| REQ-012-06 | Ubiquitous | The SecurityAnalyzer shall classify file deletion operations as 'high' risk level. |

#### 技術仕様

```typescript
interface SecurityPolicy {
  allowPatterns: string[];
  denyPatterns: string[];
  requireConfirmation: RiskLevel[];
  maxRiskLevel: RiskLevel;
}

type RiskLevel = 'low' | 'medium' | 'high' | 'critical';

interface SecurityAnalysis {
  riskLevel: RiskLevel;
  reasons: string[];
  requiresConfirmation: boolean;
  allowed: boolean;
}

class SecurityAnalyzer {
  constructor(policy: SecurityPolicy);
  analyze(action: Action): SecurityAnalysis;
  log(action: Action, result: Observation): void;
}
```

#### 受け入れ基準

- [ ] ファイル削除が「high」リスクとして評価される
- [ ] 高リスクアクションで確認プロンプトが表示される
- [ ] 禁止パターンに一致するアクションがブロックされる
- [ ] 全アクションが監査ログに記録される

---

## � 非機能要件（EARS形式）

### REQ-NFR-001: パフォーマンス

**カテゴリ**: 非機能要件（Performance）

| ID | パターン | 要件文 |
|----|---------|--------|
| REQ-NFR-001-01 | Ubiquitous | The system shall complete single tool execution within 5 seconds under normal load. |
| REQ-NFR-001-02 | State-Driven | While 10 sub-agents are running concurrently, the Orchestrator shall maintain response time under 100ms for status queries. |
| REQ-NFR-001-03 | Event-Driven | When a search query is submitted, the Workspace shall return results within 1 second for up to 10,000 files. |
| REQ-NFR-001-04 | Ubiquitous | The BrowserOperator shall complete page navigation within 10 seconds on standard broadband (10Mbps+). |

### REQ-NFR-002: スケーラビリティ

**カテゴリ**: 非機能要件（Scalability）

| ID | パターン | 要件文 |
|----|---------|--------|
| REQ-NFR-002-01 | Ubiquitous | The Orchestrator shall support 1-100 concurrent sub-agents (configurable). |
| REQ-NFR-002-02 | Ubiquitous | The Sandbox shall support memory limits from 128MB to 4GB per execution. |
| REQ-NFR-002-03 | Ubiquitous | The TaskPlanner shall handle task decomposition producing 2-50 sub-tasks. |

### REQ-NFR-003: 可用性・信頼性

**カテゴリ**: 非機能要件（Reliability）

| ID | パターン | 要件文 |
|----|---------|--------|
| REQ-NFR-003-01 | Unwanted | If a network error occurs, then the system shall retry the operation up to 3 times with exponential backoff (1s, 2s, 4s). |
| REQ-NFR-003-02 | Unwanted | If the Sandbox process crashes, then the system shall cleanup resources within 5 seconds and report the failure. |
| REQ-NFR-003-03 | Ubiquitous | The SecurityAnalyzer shall maintain 100% uptime during action processing (no bypassing). |

### REQ-NFR-004: 互換性

**カテゴリ**: 非機能要件（Compatibility）

| ID | パターン | 要件文 |
|----|---------|--------|
| REQ-NFR-004-01 | Ubiquitous | The system shall support Node.js versions 18.x, 20.x, and 22.x LTS. |
| REQ-NFR-004-02 | Ubiquitous | The BrowserOperator shall support Chromium-based browsers (Chrome 120+, Edge 120+). |
| REQ-NFR-004-03 | Ubiquitous | The Sandbox shall support Docker Engine 20.10+ and Docker Desktop 4.0+. |

### REQ-NFR-005: ロギング・監査

**カテゴリ**: 非機能要件（Auditability）

| ID | パターン | 要件文 |
|----|---------|--------|
| REQ-NFR-005-01 | Ubiquitous | The system shall log all actions with timestamp (ISO 8601), action type, parameters, and result status. |
| REQ-NFR-005-02 | Ubiquitous | The system shall support log levels: DEBUG, INFO, WARN, ERROR (configurable at runtime). |
| REQ-NFR-005-03 | Optional | Where structured logging is enabled, the system shall output logs in JSON format. |

### REQ-NFR-006: エラーハンドリング

**カテゴリ**: 非機能要件（Error Handling）

| ID | パターン | 要件文 |
|----|---------|--------|
| REQ-NFR-006-01 | Unwanted | If code compilation fails in Sandbox, then the system shall return error details including line number and error message. |
| REQ-NFR-006-02 | Unwanted | If BrowserOperator encounters a network timeout, then the system shall return a descriptive error with URL and timeout duration. |
| REQ-NFR-006-03 | Ubiquitous | The system shall use typed error classes extending a base KatashiroError for all error scenarios. |

---

## 📊 優先度マトリクス

### 機能要件

| 要件 | 優先度 | 工数（人日） | 依存関係 |
|------|--------|-------------|---------|
| REQ-006 | High | 15 | REQ-009 |
| REQ-007 | High | 10 | - |
| REQ-008 | High | 8 | - |
| REQ-009 | High | 12 | - |
| REQ-010 | Medium | 8 | - |
| REQ-011 | Medium | 10 | REQ-007 |
| REQ-012 | Medium | 6 | REQ-010 |

### 非機能要件

| 要件 | 優先度 | 工数（人日） | 適用対象 |
|------|--------|-------------|---------|
| REQ-NFR-001 | High | 3 | 全コンポーネント |
| REQ-NFR-002 | High | 2 | Orchestrator, Sandbox |
| REQ-NFR-003 | High | 4 | 全コンポーネント |
| REQ-NFR-004 | Medium | 2 | CI/CD設定 |
| REQ-NFR-005 | Medium | 3 | 全コンポーネント |
| REQ-NFR-006 | High | 3 | 全コンポーネント |

**機能要件 合計**: 69人日  
**非機能要件 合計**: 17人日  
**総合計見積もり工数**: 86人日

---

## 📅 実装ロードマップ

### Phase 1: 基盤整備（v0.4.0-alpha）
- REQ-009: タスク分解・計画
- REQ-010: Action-Observation型安全ツールシステム
- REQ-NFR-005: ログ・監査（基盤）
- REQ-NFR-006: エラーハンドリング（基盤）

### Phase 2: 実行環境（v0.4.0-beta）
- REQ-007: コード実行サンドボックス
- REQ-008: ブラウザ自動操作
- REQ-011: ワークスペース管理
- REQ-NFR-001: パフォーマンス（最適化）
- REQ-NFR-004: 互換性（テスト環境整備）

### Phase 3: 高度な機能（v0.4.0-rc）
- REQ-006: マルチエージェントオーケストレーション
- REQ-012: セキュリティ分析
- REQ-NFR-002: スケーラビリティ（負荷テスト）
- REQ-NFR-003: 信頼性（障害テスト）

### Phase 4: リリース（v0.4.0）
- 統合テスト
- ドキュメント整備
- npm公開

---

## 📋 要件サマリー

| カテゴリ | 要件数 | EARS要件数 | 工数（人日） |
|---------|--------|-----------|-------------|
| 機能要件 | 7 | 41 | 69 |
| 非機能要件 | 6 | 18 | 17 |
| **合計** | **13** | **59** | **86** |

### EARSパターン別集計

| パターン | 件数 | 割合 |
|----------|------|------|
| Ubiquitous | 8 | 13.6% |
| Event-Driven | 31 | 52.5% |
| State-Driven | 10 | 16.9% |
| Unwanted | 7 | 11.9% |
| Optional | 3 | 5.1% |

---

## 📝 変更履歴

| バージョン | 日付 | 変更内容 | 担当者 |
|-----------|------|---------|--------|
| v0.1 | 2025-01-14 | 初版作成。Manus/OpenHandsギャップ分析、機能要件定義 | GitHub Copilot |
| v0.2 | 2025-01-14 | 非機能要件追加（REQ-NFR-001〜006）、トレーサビリティ追加、用語定義追加、定量基準明確化 | GitHub Copilot |

---

## 🔗 参考資料

- [Manus Wide Research](https://manus.im/ja/features/wide-research)
- [Manus Browser Operator](https://manus.im/ja/features/manus-browser-operator)
- [OpenHands SDK Documentation](https://docs.openhands.dev/sdk)
- [OpenHands Tool System](https://docs.openhands.dev/sdk/arch/tool-system)
- [OpenHands Custom Tools](https://docs.openhands.dev/sdk/guides/custom-tools)

---

**作成者**: GitHub Copilot  
**レビューステータス**: ✅ レビュー完了（v0.2）  
**最終更新**: 2025-01-14
